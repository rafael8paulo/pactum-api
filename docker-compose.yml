services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pactum
      POSTGRES_USER: pactum
      POSTGRES_PASSWORD: pactum
    ports:
      - "5432:5432"
    volumes:
      - pactum_data:/var/lib/postgresql/data

volumes:
  pactum_data:
